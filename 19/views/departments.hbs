<!DOCTYPE html>
<html>
  <head>
    <script type="text/handlebars"></script>
    <meta charset="utf-8">
    <title>Departments</title>
  </head>
  <body>
    <h1>List of Departments</h1>
    <table>
      <thead>
        <tr>
          <th>Pulpit Name</th>
          <th>Number of Teachers</th>
        </tr>
      </thead>
      <tbody>
        {{#each departments}}
          <tr>
            <td>{{PULPIT_NAME}}</td>
            <td>{{_count.TEACHER_TEACHER_PULPITToPULPIT}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
    <div>  
{{#if true}}
  {{!-- {{#if (page > 1)}} --}}
    <a href="?page={{prevPage}}">&lt; Prev</a>
  {{!-- {{/if}} --}}
  <span>{{page}}</span>
  {{!-- {{#if (page < totalPages)}} --}}
    <a href="?page={{nextPage}}">Next &gt;</a>
  {{!-- {{/if}} --}}
{{/if}}
    </div>
  </body>
</html>